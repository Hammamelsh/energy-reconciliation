# Ideas register

Candidate work, each with the question it would answer, what is known now, what it needs,
the smallest experiment that would settle it, and a criterion for stopping. **Recording an
idea here does not authorise implementing it.** Status moves only when a ticket exists.

Evidence labels follow the project's convention: MEASURED, HYPOTHESIS, UNKNOWN.

| ID | Question / benefit | Current evidence | Prerequisites and risks | Smallest useful experiment | Success / stop criterion | Effort | Status |
|---|---|---|---|---|---|---|---|
| I-01 | **Calibrated prediction intervals.** How often does the recorded total fall inside a stated range, by horizon? | FORE-001 gives point MAE only (1.901 kWh holdout). Residual distribution by horizon is available from the same rows. HYPOTHESIS: residuals are stable enough across a run for empirical quantiles to calibrate. | Needs a holdout **not already read** to assess coverage honestly (FORE-001's has been). Risk: intervals wide enough to cover 975 zero-days are useless; level shifts within a run break calibration. | Empirical quantile intervals from development residuals per model per horizon; report **coverage and mean width by horizon** on a new holdout window or a new cohort. | Nominal 80% interval achieves 75–85% coverage on holdout at every horizon; stop if width exceeds the target's median. | S | idea |
| I-02 | **One additional forecasting baseline, with a stated hypothesis.** Does a recency-weighted same-weekday average beat the equal-weight 4-week mean? | MEASURED on the common frame: 1/2/4/8 weeks → 2.330/2.144/2.059/2.119. HYPOTHESIS: 8 loses to 4 because older weeks are less representative; if so, exponential weighting should beat 4 at the same span. The alternative (variance from window length) predicts no gain. | The existing holdout has been read; selection must be on development and confirmed on a fresh window. Risk: adding models without hypotheses becomes a leaderboard. | Exponentially weighted same-weekday mean with 2–3 decay values plus weeks ∈ {3, 5, 6}, on the **same common frame**, selected on development only. | Beats 4-week equal-weight MAE on the common frame **and** on a fresh holdout; otherwise record the null and stop. | S | idea |
| I-03 | **Weather / calendar features available at the origin.** Does information the household's own past cannot contain reduce error on the days the models miss? | HYPOTHESIS only. FORE-001 §6 shows errors concentrate on one-off days; no external input has been tried. Not established that weather is the highest-gain input. | A licensed daily temperature series for the trial area; **forecast-time information only** — observed future weather must not stand in for a forecast that would have existed on the origin date. A holiday calendar needs the unresolved timestamp convention to align dates. Risk: joining on source dates assumes the convention, which forecasting does not establish. | Add day-of-year seasonality first (no external data), then a same-day-as-origin temperature lag (known at origin). Measure the change on a fresh holdout. | A measured MAE reduction on the fresh holdout larger than the difference between the two existing baselines; stop if seasonality alone accounts for it. | M | idea |
| I-04 | **Half-hourly forecasting.** Is the within-day shape predictable, and does it change a scenario's band mix? | Half-hourly readings are ingested and policy-cleaned. UNKNOWN whether shape is predictable beyond a weekday profile. | A separate **336-step weekly evaluation** (48 × 7) with its own eligibility; per-step MAE will be dominated by peaks. Any link to scenario charges needs the tariff-alignment assumption **A1 stated explicitly** for the forecast as well; forecasting does not establish it. | Same-weekday half-hour profile from the previous week vs a 4-week profile mean; report MAE by half-hour of day and by horizon day. | Profile mean beats naive profile on a fresh holdout; if the band-mix question is pursued, A1 must be carried on every output row. | M | idea |
| I-05 | **Late and revised readings.** Can a corrected reading produce an explained delta, not a silent overwrite? | REC-001 already models removals and changed values in the comparison; a revision is a changed value at an existing key. MEASURED: the machinery reconciles to zero residual for source expansion. | A source of revisions — none exists in the archive; must be synthetic and labelled. Risk: synthetic revisions described as real evidence. | Synthetic member revising ten readings; comparison must show ten changed values, their old/new sources, and a residual of zero. | The explained delta equals the recomputed total exactly; every changed row carries both source references. | S | idea |
| I-06 | **dbt port (ANL-003).** Does one definition of the policies survive being expressed in dbt? | The tariff SELECTs are written and tested. The port is a design task: model boundaries, `ref`/`source` wiring, the non-SQL steps, rerun policy, run identity. | A decided answer for keeping the shared policy to one definition across Python and dbt; a captured baseline must still replay and match. Risk: two implementations of one rule. | Port `dim_tariff_band_schedule` and `dim_tariff_price` only, with dbt tests for the PK and band domain, and replay the accepted baseline. | `dbt build` reproduces ANL-002's figures exactly for the ported models; policy still has one definition. | M | **designed** — `anl-003-dbt-design.md`, ticket `tickets/ANL-003-dbt-port.md`; not built |
| I-07 | **Geography.** Is any geographic breakdown legitimate? | UNKNOWN. No metadata is linked; none was researched. | **Only** if appropriately licensed metadata supports a valid join to these household ids. Risk: inferring location from anything else. | None until such metadata exists; the first step is establishing whether it does, and under what licence. | A documented, licensed join key; otherwise the idea stays closed. | — | blocked |
| I-08 | **Prior-data eligibility evaluation.** *(implemented — see `i-08-prior-data-eligibility.md`)* What does accuracy look like when eligibility at each origin is decided from data behind that origin only? | FORE-001 is a retrospective clean-run benchmark (MEASURED: 30 of 40 runs end before the warehouse does). UNKNOWN how the figures move; prior-data eligibility does **not** guarantee a worse MAE and does **not** by itself isolate selection bias, because a household's later data quality still decides whether its targets can be *scored*. | Freeze the origins, horizon and model settings first. Distinguish a **forecast decline** (the model lacked a usable input) from an **unusable future target** (nothing to score against). The warehouse supports an **as-of-source-date simulation** only: ingestion and publication timing are not recorded, so no claim of historical production availability may be made. The already-inspected holdout keeps that status. | Same three models, same frozen origins. At each origin, require only a usable 28-day window *behind* it. Report **prediction coverage** (share of origin×horizon cases with a forecast) and **scoring coverage** (share with a usable target) separately. Compare MAE on the cases **common** to FORE-001 separately from the **newly included** cases. | Both coverages and both MAEs reported side by side; the common-case MAE must reproduce FORE-001's for the same models; the conclusion is written from the newly-included cases only. | S | **done** |
| I-09 | **Household-equal-weight reporting.** Should MAE give each household equal weight? | MEASURED: development pooled 2.094 vs household-equal 2.055 (counts 112–378 per household); holdout identical by construction. | None. Risk: two headline numbers without saying which is which. | Report both, labelled, in the experiment output. | Both figures shown wherever one is; done when the tab carries the label. | XS | idea |
| I-12 | **Why does persistence overtake the one-week naive on the broader population?** | MEASURED (I-08): on 57,885 common cases `persistence_1` 2.250 beats `seasonal_naive_7` 2.319, reversing FORE-001's cohort ordering. HYPOTHESIS: on patchier histories a seven-day-old value is more often stale than yesterday's. | Needs a decomposition that separates the newly included households from the new origin calendar; both changed at once in I-08. | Re-run I-08 twice: once with FORE-001's 40 households on the shared calendar, once with all 83 on FORE-001-style origins. Compare the ordering in each. | If the ordering flips in only one of the two, that isolates which change causes it; if in both or neither, record the null. | S | idea |
| I-11 | **Weekday-class baseline.** Does a reference that respects the weekday/weekend boundary beat persistence, and by how much of the gap to the weekday models? | MEASURED: persistence MAE is 2.665 (holdout) when origin and target cross the weekday/weekend boundary vs 2.208 same-side, different weekday; 1.987 same weekday. HYPOTHESIS: most of persistence's disadvantage is that crossing. | A new reference must be stated as a hypothesis before scoring; the read holdout must not be used to select it. | Persistence restricted to the last usable day *of the same weekday class* (weekday/weekend) at or before the origin; score on development, common frame. | If it closes more than half the gap between persistence and the 1-week seasonal naive on development, record it; otherwise record the null. | XS | idea |
| I-13 | **Classify large weekday-mean errors by input pattern.** Cohort-wide, are the 4-week mean's worst misses days unlike *all four* of their inputs, or days where *one* input week was itself anomalous? | MEASURED on MAC000131's top errors (worst-days table, 2026-09-08): Sat 9 Nov 2013 observed 17.965 against inputs 9.544 · 6.706 · 7.253 · 11.546 (mean 8.762) — outside the range of all four; Wed 25 Dec 2013 observed 21.369 against 14.205 · 11.965 · 12.638 · 14.445 (13.313) — likewise. No case in the top ten is a one-anomalous-week miss. HYPOTHESIS: the mean's damping helps only in the second kind, which may be rare. | The prediction record already stores the input dates (`Row.inputs`), so no new modelling is needed. Risk: a threshold for "outside the range" is a choice and must be stated first. | Over every scored `weekday_mean_4` case in FORE-001 development, label each error by whether the target lies outside [min, max] of its four inputs; report MAE and count per label. | If more than half of the top-1% errors are outside-range misses, record that averaging cannot address them and that the gap is an input, not a window length; otherwise record the null. | XS | idea |
| I-14 | **Keeping the dashboard readable while a scenario is rebuilt.** What should a reader see while a build holds the warehouse, and afterwards? | MEASURED (duckdb 1.5.5, two processes, 2026-09-08): while one process holds a warehouse **read-write**, another cannot open it **even read-only** (`IOException: Could not set lock on file`); while one holds it read-only, further read-only opens succeed and any read-write open is refused. So the lock is on the **file**, and ANL-003's `scenario_build` staging schema does not help: atomic publication and reader availability are different problems. | Must not weaken the D5 guarantee that a failed build leaves the last published result **and its identity** intact. Risk: a reader pointed at a stale copy with no way to tell it is stale — worse than a brief outage, because it is silent. | Compare three options on the real warehouse: (a) a short declared write window with the reader retrying and saying so; (b) the reader opening a published copy, stamped with the `run_id` it came from; (c) dbt building in a separate file, with only the publish touching the warehouse. Measure the write window's length for a full rebuild. | An option where the reader is never silently stale and the unavailable window is stated in seconds; if all three are worse than simply announcing the outage, record that and keep the outage. | S | **decided** — option (c) chosen at the step-5 review: immutable versioned whole-warehouse files + a manifest replaced by atomic rename; there is **no** unavailable window because the builder never opens the published file. Proved with separate processes, then **implemented as `publication.py`** (`tests/test_publication.py`); see design D5. Remaining: `build-candidate` and the dashboard switch. |
| I-15 | **Restoring declared constraints on dbt-built tables.** `models.SCHEMA` declares `PRIMARY KEY` and `NOT NULL` on both tariff dimensions. Should the dbt-built versions carry them too, or is a dbt test enough? | MEASURED 2026-09-08: dbt materialises a table with `CREATE TABLE AS SELECT`, which carries column names and types and **drops constraints**. The dbt-built dimensions have `DECIMAL(9,4)`/`DECIMAL(9,6)` exactly as declared but **no key and no NOT NULL**. The duplicate-label refusal is unaffected — it runs in `schedule._validate` before a row exists — so what is lost is the declaration, not the protection. | dbt model contracts (`+contract: {enforced: true}` with a `columns:` block) can emit constraints, but they require every column's type to be declared in YAML, which is a second place the schema is written down — the exact duplication ANL-003 exists to avoid. Risk: the YAML and `models.SCHEMA` drift. | Enable a contract on `dim_tariff_price` only, generating the `columns:` block from `dimensions.PRICE_SCHEMA` the way the policy macro is generated from `policy.py`, and check a duplicate insert is refused by the database rather than by a test. | Constraints present in `information_schema` **and** the YAML generated, not hand-kept; if the generator turns out to cost more than the dbt uniqueness tests are worth, record that and keep the tests. | S | idea |
| I-16 | **Making a candidate build's status readable from the data.** After a partial rebuild, how does a reader tell which candidate tables are current? | MEASURED 2026-09-08: dbt fails per model, so a failed run leaves independently successful models rebuilt while `scenario_build.dbt_build_run` still shows the previous success. `run-dbt` returns non-zero and records nothing for a failed attempt, so the log never *claims* a failed build succeeded — but it also cannot describe what the schema holds afterwards. | Must not become a second source of truth about the tables, and must not be mistaken for the published `scenario_run`. Risk: a status column that is itself stale is worse than none. | Stamp each candidate table with the `run_id` that wrote it (the facts already carry one) and have `run-dbt` report, after any build, which tables carry the current run_id and which carry an older one. | A reader can tell current from stale without consulting dbt's logs; if the step-5 publisher makes the question moot by publishing atomically, close this instead. | XS | **closed by design** — under D5 every build attempt is a **new file**, so a later failed attempt can never change tables an earlier success row describes; the question does not arise. The stale-certificate caveat remains true only for the current unpublished `scenario_build` schema, which step 5 retires. |
| I-17 | **A confirmed command for deleting old published versions.** When is it safe to remove a version file, and what must the command show before it does? | MEASURED (proof, 2026-09-08): an open DuckDB connection survives `unlink` on ext4; a *new* open of the same path fails. The dashboard opens a fresh connection per query to the path it resolved at the start of a render, so deleting a version mid-render breaks that render. `publication.inventory` is the read-only dry run; nothing deletes today. | Must never remove the published or previous version, anything younger than a grace window, or a file another process has open. Risk: a grace window that is a guess rather than a measurement of the longest real render. | Measure the longest real dashboard render on the three-member warehouse; write `publication sweep --keep N --grace T` that prints exactly what it would delete and requires `--yes`; run it against a root with an open reader. | Deletes only what the dry run listed, never a protected or open file, and refuses without `--yes`. | XS | idea |
| I-18 | **Should a candidate record which dbt nodes actually ran?** A build record proves a build succeeded; it does not say which models that run rebuilt. | MEASURED 2026-09-08: dbt fails per node, and the *shape* of a failure decides what a later guard can see. A failure on the first model rebuilds the independent models to identical contents and skips the rest, so `built_output_sha256` is unchanged and cannot object; a failure at a test after the models rebuilt does move the tables and is caught. Both are regression-tested. | dbt already writes `run_results.json` with every node and its status; storing it would grow the record and duplicate what dbt owns. Risk: a second, drifting copy of dbt's own result. | Store the run-results node/status pairs (or their digest) in `dbt_build_run`, and have `finalise` refuse when the recorded node set does not cover every model in the project. | Refuses a candidate whose record describes fewer models than the project defines; if the fresh-file lifecycle already makes that unreachable in practice, record the null and close. | XS | idea — **narrowed 2026-09-08**: every `run-dbt` attempt is now recorded (`started` → `succeeded`/`failed`) and `finalise` requires the latest to have succeeded, so "was the last attempt the success on record?" is answered from the data. Still open: *which nodes* a succeeded build ran. |
| I-10 | **Hindsight-free cohort with balanced groups.** Does the `Std`/`ToU` imbalance (35/5) or the member skew (34/40 from members 4–5) change the result? | MEASURED composition; UNKNOWN effect. | Balancing by group is itself a selection rule and must be stated first. | Re-run on the 24 eligible `ToU` households as a second cohort; compare pooled and per-household MAE. | Report both cohorts; if orderings differ, the result is cohort-dependent and the doc says so. | S | idea |

## Not ideas

Things that are excluded on purpose rather than pending: appliance detection, causal
claims about tariff response, presenting any scenario charge as a bill or a saving, and an
AI assistant that alters readings or calculates bills.
